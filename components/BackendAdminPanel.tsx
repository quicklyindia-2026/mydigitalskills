"use client";
import{useState}from"react";
type Lead={id:string;name:string;phone:string;service:string;business:string|null;status:string;createdAt:string};type Blog={id:string;title:string;status:string;category:string};type Portfolio={id:string;title:string;sector:string;status:string};
export function BackendAdminPanel({leads,blogs,portfolio}:{leads:Lead[];blogs:Blog[];portfolio:Portfolio[]}){const[msg,setMsg]=useState("");async function send(e:React.FormEvent<HTMLFormElement>,url:string,method="POST"){e.preventDefault();setMsg("Saving…");const payload=Object.fromEntries(new FormData(e.currentTarget).entries());const r=await fetch(url,{method,headers:{"content-type":"application/json"},body:JSON.stringify(payload)}),j=await r.json();if(!r.ok){setMsg(j.error||"Could not save.");return}setMsg("Saved successfully.");location.reload()}async function leadStatus(id:string,status:string){const r=await fetch("/api/admin/enquiries",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({id,status})});if(r.ok)location.reload()}
return <div className="admin-stack backend-sections">{msg&&<div className="admin-message">{msg}</div>}
<section id="leads" className="admin-card"><div className="admin-title-row"><div><p className="eyebrow">CRM</p><h2>Consultation Leads</h2></div><span className="admin-count">{leads.length}</span></div>{leads.length?<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Name</th><th>Contact</th><th>Service</th><th>Status</th><th>Action</th></tr></thead><tbody>{leads.map(x=><tr key={x.id}><td><strong>{x.name}</strong><small>{x.business||"Personal"}</small></td><td><a href={`tel:${x.phone}`}>{x.phone}</a></td><td>{x.service}</td><td><span className={`status-pill ${x.status}`}>{x.status}</span></td><td><select value={x.status} onChange={e=>leadStatus(x.id,e.target.value)}><option value="new">New</option><option value="contacted">Contacted</option><option value="qualified">Qualified</option><option value="won">Won</option><option value="closed">Closed</option></select></td></tr>)}</tbody></table></div>:<p>No consultation leads yet.</p>}</section>
<div className="admin-grid" id="content"><form className="admin-card form-grid" onSubmit={e=>send(e,"/api/admin/blogs")}><h2 className="field full">Create Blog Post</h2><label className="field"><span>Title</span><input name="title" required/></label><label className="field"><span>SEO Slug</span><input name="slug" placeholder="your-blog-title" required/></label><label className="field"><span>Category</span><input name="category" defaultValue="Business Growth"/></label><label className="field"><span>Status</span><select name="status"><option value="draft">Draft</option><option value="published">Published</option></select></label><label className="field full"><span>Short excerpt</span><textarea name="excerpt" required/></label><label className="field full"><span>Article content (paragraphs)</span><textarea name="content" rows={10} required/></label><label className="field full"><span>Cover image path</span><input name="coverImage" placeholder="/images/blog-cover.webp"/></label><label className="field"><span>SEO title</span><input name="seoTitle"/></label><label className="field"><span>SEO description</span><input name="seoDescription"/></label><button className="button button-dark field full">Save Blog</button></form>
<form className="admin-card form-grid" onSubmit={e=>send(e,"/api/admin/portfolio")}><h2 className="field full">Add Portfolio / Case Study</h2><label className="field"><span>Industry</span><input name="sector" placeholder="Hospitality & F&B" required/></label><label className="field"><span>Project title</span><input name="title" required/></label><label className="field full"><span>Description</span><textarea name="description" required/></label><label className="field full"><span>Work delivered (one item per line)</span><textarea name="work" rows={7}/></label><label className="field"><span>Image path</span><input name="imageUrl" placeholder="/images/project.webp"/></label><label className="field"><span>Project link</span><input name="projectUrl" placeholder="https://..."/></label><label className="field"><span>Order</span><input name="sortOrder" type="number" defaultValue="1"/></label><label className="field"><span>Status</span><select name="status"><option value="published">Published</option><option value="draft">Draft</option></select></label><button className="button button-dark field full">Add Case Study</button></form></div>
<div className="admin-grid"><section className="admin-card"><h2>Blog Library</h2>{blogs.length?<div className="admin-list">{blogs.map(x=><div className="admin-row" key={x.id}><div><strong>{x.title}</strong><span>{x.category}</span></div><span className={`status-pill ${x.status}`}>{x.status}</span></div>)}</div>:<p>New backend-created blogs will appear here.</p>}</section><section className="admin-card"><h2>Portfolio Library</h2>{portfolio.length?<div className="admin-list">{portfolio.map(x=><div className="admin-row" key={x.id}><div><strong>{x.title}</strong><span>{x.sector}</span></div><span className={`status-pill ${x.status}`}>{x.status}</span></div>)}</div>:<p>New case studies will appear here.</p>}</section></div>
<form className="admin-card form-grid" onSubmit={e=>send(e,"/api/admin/content")}><h2 className="field full">Website Content Setting</h2><p className="field full">Save reusable website text, contact details, announcements or image paths by a unique key.</p><label className="field"><span>Content key</span><input name="key" placeholder="homepage_announcement" required/></label><label className="field"><span>Label</span><input name="label" placeholder="Homepage announcement"/></label><label className="field"><span>Type</span><select name="type"><option value="text">Text</option><option value="image">Image path</option><option value="link">Link</option></select></label><label className="field full"><span>Value</span><textarea name="value" required/></label><button className="button button-primary field full">Save Website Content</button></form></div>}
